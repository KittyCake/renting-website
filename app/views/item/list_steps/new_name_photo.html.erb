<%= form_for @item, method: :put, url: wizard_path do |f| %>
  <% if f.object.errors.any? %>
    <div class="error_messages">
      <% f.object.errors.full_messages.each do |error| %>
        <p><%= error %></p>
      <% end %>
    </div>
  <% end %>

  <fieldset>
    <legend>Item Name and Photo</legend>

    <div>
      <%= f.label :item_name %>
      <%= f.text_field :item_name %>
    </div>

    <div>
      <%= f.label :item_photo %>
      <%= f.text_field :item_photo %>
    </div>

    <div>
      <%= f.submit 'Next Step' %>
    </div>
  </fieldset>
<% end %>
